import {SafeAreaView, ScrollView, StatusBar} from "react-native";

const AddBottle = () => {
    return <SafeAreaView>
        <StatusBar />
        <ScrollView>

        </ScrollView>
    </SafeAreaView>
}

export default AddBottle;